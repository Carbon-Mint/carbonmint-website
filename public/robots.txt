# Allow all crawlers
User-agent: *
Allow: /

# Disallow admin or private areas (add if needed)
# Disallow: /admin/
# Disallow: /api/

# Sitemap location
Sitemap: https://carbonmint.com/sitemap.xml

# Crawl-delay (optional, helps prevent server overload)
Crawl-delay: 1
